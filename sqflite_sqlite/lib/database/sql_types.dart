class SQLTypes {
  static const id = 'INTEGER PRIMARY KEY AUTOINCREMENT';
  static const integer = 'INTEGER NOT NULL';
  static const boolean = 'BOOLEAN NOT NULL';
  static const text = 'TEXT NOT NULL';
}